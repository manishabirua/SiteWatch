# SiteWatch

A website uptime monitor. Checks URLs every 5 minutes, 
logs UP/DOWN status, displays results on a live dashboard.

## Stack
- Python (checker script)
- Flask (dashboard)
- Redis (storage)
- Docker + docker-compose
- nginx (reverse proxy)
- AWS EC2 (deployment)
- GitHub Actions (CI/CD)# My SiteWatch Project
